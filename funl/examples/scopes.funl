a = int(0)

my_func = {
    b = int(1)

    print("a() from global env: ")
    print(cast(a(), str), n)
    print("b() from local env: ")
    print(cast(b(), str), n)
}

my_func()

print(cast(b(), str))

